BENCHMARK_TARGETS := Runtime StaticSC StaticSCPin StaticMaxGoto StaticMaxGotoLTO

BENCH_FLAGS_Runtime := -DINTERPRET_SWITCH -DBUS_UNPINNED -fno-lto
BENCH_FLAGS_StaticSC := -DSTATIC_SYSTEM -DINTERPRET_SWITCH -DBUS_UNPINNED -fno-lto
BENCH_FLAGS_StaticSCPin := -DSTATIC_SYSTEM -DINTERPRET_SWITCH -fno-lto
BENCH_FLAGS_StaticMaxGoto := -DSTATIC_SYSTEM -fno-lto
BENCH_FLAGS_StaticMaxGotoLTO := -DSTATIC_SYSTEM -flto

BENCH_LDFLAGS_Runtime :=
BENCH_LDFLAGS_StaticSC :=
BENCH_LDFLAGS_StaticSCPin :=
BENCH_LDFLAGS_StaticMaxGoto :=
BENCH_LDFLAGS_StaticMaxGotoLTO := -flto
